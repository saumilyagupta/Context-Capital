# Context Capital

Phase-1 reference client for the Context Protocol — user-owned, cross-vendor AI memory portability.

## Installation

```bash
pip install -e ".[dev]"
```

## Development

Run tests:

```bash
pytest tests/ -v
```
